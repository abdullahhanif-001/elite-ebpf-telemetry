# compose-netstacklat

Enable netstacklat example configs from pinned bpf-examples (build requires clang). Not auto-started.

```bash
sudo bash scripts/oneclick/elite-oneclick.sh enable netstacklat
```

See ADR-003 — config vendored by commit; operator builds BPF object separately.
